
name: CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: Gr1N/setup-poetry@v8
      - name: Setup Poetry
        working-directory: ./gces-bib
        run: |
          poetry build
        
